import { CSSProperties } from 'react';

export const baseStyles: CSSProperties = {
  position: 'absolute',
  opacity: 0,
  pointerEvents: 'none',
  top: 0,
  left: 0,
  userSelect: 'none',
  transform: 'translate3d(0,0,0)',
};
