/** eslint-disable no-shadow */

export enum KeyCodes {
  ESCAPE = 'Escape',
  ENTER = 'Enter',
  TAB = 'Tab',
  UP_ARROW = 'Up',
  DOWN_ARROW = 'Down',
}

export enum MouseButton {
  LEFT = 0,
  RIGHT = 2,
}
